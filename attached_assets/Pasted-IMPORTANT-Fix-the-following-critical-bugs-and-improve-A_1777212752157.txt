IMPORTANT: Fix the following critical bugs and improve AI teaching quality:
FIX 1 — AI Must Never Give Direct Answers — Full Teaching Mode:
The current AI is giving direct answers to homework questions without proper explanation. This must be completely fixed. Update Adam’s system prompt with these strict rules:
When a child asks any homework question or academic problem, Adam must NEVER state the answer directly. Adam must always follow this exact teaching sequence: First acknowledge the question enthusiastically. Second break the problem into the smallest possible step and ask the child about only that first step. Third wait for the child’s response before moving to the next step. Fourth if the child answers correctly celebrate and move to the next step. Fifth if the child answers incorrectly give a helpful hint using a real-life example appropriate for their age and try again. Only reveal the final answer after the child has worked through all the steps themselves, and frame it as “YOU figured it out!” not “the answer is.”
Example of wrong behavior: Child asks “what is 6+4?” and Adam says “6+4=10”. This is completely forbidden.
Example of correct behavior: Child asks “what is 6+4?” and Adam says “Ooh great question detective! 🕵️ Let’s figure this out together! If you had 6 candies in one hand — can you picture them? Now someone gives you 4 more candies. Let’s count together from 6… what comes after 6? 🍬”
This rule applies to ALL subjects: math, Arabic, English, science, geography, and every other subject. No exceptions.
FIX 2 — Fix Audio Overlap and Glitch in Chat:
There is a critical audio bug where pressing the speaker button a second time while audio is already playing causes two audio streams to play simultaneously creating a horrible glitch sound. Fix this completely with the following logic:
Before playing any new audio, always check if any audio is currently playing. If audio is currently playing and the user presses the speaker button: immediately stop the current audio completely, reset the audio player state to zero, then start the new audio from the beginning. Never allow two audio instances to play at the same time under any circumstances. Add a global audio manager that controls all audio playback across the entire app — only one audio source can be active at any moment. When new audio starts, the manager must kill all other audio immediately before starting the new one.
FIX 3 — Fix Audio Continuing Between Games:
When a child navigates away from one game to another game or any other screen, all audio from the previous screen must stop completely and immediately. Fix this by: adding an audio cleanup function that runs every time any screen unmounts or the user navigates away. This cleanup function must stop all currently playing audio, clear all audio queues, and reset the audio player state. The cleanup must run on every route change without exception. Test specifically: leaving Word Puzzle while audio is playing must immediately silence it. Leaving Math Blast while a sound effect is playing must immediately silence it. Navigating from any game to home screen must immediately silence all game audio.
FIX 4 — Fix Push to Talk Speech Recognition Accuracy:
The Push to Talk is transcribing audio incorrectly — when the child says “I didn’t understand” it writes something completely different. Fix the Whisper API implementation with the following improvements:
Add language detection hint to the Whisper API call — if the app is in Arabic mode send language parameter as “ar”, if English mode send as “en”. This dramatically improves accuracy. Add a minimum recording duration of 1.5 seconds — if the child releases the button in less than 1.5 seconds show “Hold the button longer while you speak! 🎤” and do not send to Whisper. Increase the audio recording quality — use audio constraints: sampleRate 16000, channelCount 1, echoCancellation true, noiseSuppression true, autoGainControl true. These settings are specifically optimized for speech recognition. After receiving the Whisper transcription, before sending to Adam, run a simple validation: if the transcription is less than 2 words or contains only punctuation, show “I didn’t catch that — try again! 🎤” and do not send to Adam. Add visual feedback showing the recording quality — a simple waveform animation that reacts to the child’s voice volume so they know the microphone is picking up their voice properly.